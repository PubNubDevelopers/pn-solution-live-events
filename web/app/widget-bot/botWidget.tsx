export default function BotWidget ({
  className,
  isMobilePreview,
  chat,
  guidesShown,
  visibleGuide,
  setVisibleGuide
}) {
  return (
    <div className={`${className}`}>
      <div className=''>I am the Bot Widget.</div>
    </div>
  )
}
